# Swag Labs UI Test Suite

Automated UI tests for the Swag Labs demo site using Python + Playwright with the Page Object Model.

## Setup

```bash
pip install -r requirements.txt
playwright install
```

## Run Tests

```bash
pytest -v
```